// Moved to src/components/shared/quick-action.tsx (shared admin + client). Re-export shim
// keeping the legacy AdminQuickAction name so existing admin imports keep working.
export { QuickAction as AdminQuickAction } from "@/components/shared/quick-action";
