// Moved to src/components/shared/empty-state.tsx (shared admin + client). Re-export shim
// keeping the legacy AdminEmptyState name so existing admin imports keep working.
export { EmptyState as AdminEmptyState } from "@/components/shared/empty-state";
