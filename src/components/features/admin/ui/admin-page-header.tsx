// Moved to src/components/shared/page-header.tsx (shared admin + client). Re-export shim
// keeping the legacy AdminPageHeader name so existing admin imports keep working.
export { PageHeader as AdminPageHeader } from "@/components/shared/page-header";
